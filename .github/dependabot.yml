version: 2

updates:
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "03:00"
      timezone: Asia/Shanghai
    # Solo mainline development uses direct reviewed commits; dependency
    # updates are run manually and Dependabot must not create update PRs.
    open-pull-requests-limit: 0
    groups:
      cargo-maintenance:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
    ignore:
      - dependency-name: "*"
        update-types:
          - version-update:semver-major
    rebase-strategy: auto
    commit-message:
      prefix: deps
      include: scope

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "03:30"
      timezone: Asia/Shanghai
    open-pull-requests-limit: 0
    groups:
      github-actions-maintenance:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
    ignore:
      # The action ref is the repository's pinned Rust release, not an
      # independently upgradeable action version.
      - dependency-name: "dtolnay/rust-toolchain"
      - dependency-name: "*"
        update-types:
          - version-update:semver-major
    rebase-strategy: auto
    commit-message:
      prefix: ci
      include: scope
