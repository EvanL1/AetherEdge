# Vendored SunSpec model definitions

`aether-sunspec/build.rs` consumes only `json/model_*.json`. The files provide
the optional SunSpec model catalog and are licensed under the adjacent
Apache-2.0 `LICENSE` file. Upstream generation tooling and alternate
XML/workbook representations are intentionally not vendored.
